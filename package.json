{
  "name": "speakx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && node app.js\" \"cd Client && npm run dev\"",
    "install-all": "npm install concurrently && concurrently \"cd Client && npm install\" \"cd server && npm install\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}